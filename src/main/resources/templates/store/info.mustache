<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>가게 정보</title>
    <script src="/script/infoConfig.js"></script>
</head>
<body>
    <button onclick="backButton()">Back</button>
    

    <div><img src="/img/store/burgers_taein.png" style="width:30vh;"></div>

    
    
    {{#storeData}}
    <h3>{{name}}</h3>
    <div style="margin-bottom:5vh;">
        <span>{{style}}</span>
        <span>{{location}}</span> <a href="https://map.naver.com/p/entry/place/{{attr}}">네이버 지도</a>
    </div>
    {{/storeData}}

    {{#storeTimeData}}
    <div>
        <span>{{DoW}}</span>
        <span>영업 시간: {{startTime}} ~ {{endTime}}</span>
        <span>쉬는 시간: {{breakStart}} ~ {{breakEnd}}</span>
    </div>
    {{/storeTimeData}}

    <table>
        {{#menuData}}
            <tr>
                <td>{{name}}</td>
                <td>{{price}}</td>
            </tr>
        {{/menuData}}
    </table>
</body>
</html>