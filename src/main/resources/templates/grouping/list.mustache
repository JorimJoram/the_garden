<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>그루핑</title>
</head>
<body>
{{#groupList}}
    <div>
        <a href="http://localhost:12571/grouping/detail/{{id}}">{{title}}</a>
        <span>{{formattedDate}}</span>
        <span>{{account.username}}</span>
        <div>
            <span>{{content}}</span>
        </div>
    </div>
{{/groupList}}
</body>
</html>