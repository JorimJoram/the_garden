<!DOCTYPE html>
<html lang="en">
<head>
    {{>layout/header}}
    <title>그루핑</title>
</head>
<body>
{{>layout/bottom_navi}}
<div class="garden_container">
    {{#groupList}}
        <div>
            <a href="/grouping/detail/{{id}}">{{title}}</a>
            <span>{{formattedDate}}</span>
            <span>{{account.username}}</span>
            <div>
                <span>{{content}}</span>
            </div>
        </div>
    {{/groupList}}
</div>

</body>
</html>